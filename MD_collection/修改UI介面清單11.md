# 修改UI介面清單11 - 預設顯示轉錄測試資料

## 目標
在頁面載入時，預設顯示以下轉錄資料，以便使用者預覽介面效果。

## 預設資料內容
1. **時間**: 0:00 - 0:00
   - **內容**: ⚠️ 遠端未回應，啟用本地辨識備援。
   - **備註**: 帶有警告圖示
2. **時間**: 0:00 - 0:03
   - **內容**: 測試測試
3. **時間**: 0:03 - 0:04
   - **內容**: 測試。

## 實作步驟
1. **重構 `js/presentation.js`**:
   - 提取 `createTranscriptRow(startSec, endSec, text)` 函式，用於產生轉錄列 DOM 元素。
   - 修改原有的 `appendTranscript` 函式改為呼叫 `createTranscriptRow` 並插入頁面。

2. **新增 `initDummyData` 函式**:
   - 在 `DOMContentLoaded` 事件中呼叫此函式。
   - 手動呼叫 `createTranscriptRow` 並插入上述三筆預設資料到 `#liveTranscription_presentation`。

3. **資料細節**:
   - 第一筆: Start: 0, End: 0, Text: "⚠️ 遠端未回應，啟用本地辨識備援。"
   - 第二筆: Start: 0, End: 3, Text: "測試測試"
   - 第三筆: Start: 3, End: 4, Text: "測試。"

## 驗證
- 重新整理頁面，確認「本地語音轉錄」區域出現預設的三筆資料。
- 確認時間與文字內容與需求一致。
